[workspace]

[package]
name = "p0-03-ort-cpu-session-spike"
version = "0.0.0"
edition = "2024"
publish = false
description = "Issue #4 (P0-03) technical spike: explicit-load ORT C API CPU Session on Windows ARM64. Not production code (see README Non-goals)."

# No external crates: kernel32 and the ONNX Runtime C API are called through
# hand-declared `extern` FFI so the spike builds fully offline once the
# pinned onnxruntime.dll/headers are on disk (see README.md for provenance).
[dependencies]
